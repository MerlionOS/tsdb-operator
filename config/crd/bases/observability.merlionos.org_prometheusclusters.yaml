---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: prometheusclusters.observability.merlionos.org
spec:
  group: observability.merlionos.org
  names:
    kind: PrometheusCluster
    listKind: PrometheusClusterList
    plural: prometheusclusters
    singular: prometheuscluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: PrometheusCluster is the Schema for the prometheusclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PrometheusCluster
            properties:
              additionalScrapeConfigs:
                description: |-
                  AdditionalScrapeConfigs supplies user-side scrape configuration
                  merged into the generated prometheus.yml via the native
                  scrape_config_files mechanism (Prometheus 2.43+).
                  Exactly one of `inline` or `secretRef` may be set.
                properties:
                  inline:
                    description: |-
                      Inline is a top-level YAML list of scrape entries, e.g.

                        - job_name: my-app
                          static_configs:
                            - targets: [my-app:8080]

                      The operator wraps it under a `scrape_configs:` key in the
                      generated ConfigMap. Use this when the config is small enough to
                      live in the CR.
                    type: string
                  secretRef:
                    description: |-
                      SecretRef references a Secret containing a fully-formed Prometheus
                      scrape config file (must already include the top-level
                      `scrape_configs:` key). The Secret is mounted into the Prometheus
                      container at /etc/prometheus/extra-secret/<key>. Use this for
                      configs too large or sensitive to inline.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              backup:
                description: Backup configuration.
                properties:
                  bucket:
                    type: string
                  credentialsSecretRef:
                    description: CredentialsSecretRef references a Secret with AWS_ACCESS_KEY_ID
                      / AWS_SECRET_ACCESS_KEY.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  enabled:
                    type: boolean
                  endpoint:
                    description: Endpoint overrides the S3 endpoint (e.g. for MinIO).
                    type: string
                  prefix:
                    description: Prefix under which snapshot objects are stored.
                    type: string
                  region:
                    type: string
                  schedule:
                    description: |-
                      Cron expression for scheduled snapshots. Standard 5-field cron
                      syntax; deeper validation is done by the webhook.
                    minLength: 1
                    type: string
                type: object
              image:
                default: prom/prometheus:v2.55.1
                description: Image is the Prometheus container image.
                type: string
              remoteWrite:
                description: RemoteWrite endpoints the managed Prometheus should stream
                  samples to.
                items:
                  description: |-
                    RemoteWriteSpec declares a Prometheus remote_write target. Fields mirror
                    the subset of the upstream Prometheus remote_write schema we render into
                    the generated prometheus.yml.
                  properties:
                    basicAuthSecretRef:
                      description: BasicAuthSecretRef references a Secret with keys
                        "username" and "password".
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    bearerTokenSecretRef:
                      description: BearerTokenSecretRef references a Secret with key
                        "token".
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    name:
                      description: Name is an optional label for the queue (used in
                        Prometheus metrics).
                      type: string
                    url:
                      description: URL is the remote endpoint to ship samples to.
                      minLength: 1
                      type: string
                  required:
                  - url
                  type: object
                type: array
              replicas:
                default: 1
                description: Replicas is the desired number of Prometheus replicas.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources is the container resource requests/limits.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              retention:
                default: 15d
                description: |-
                  Retention period for local TSDB data, e.g. "15d".
                  Accepts Prometheus duration syntax: [0-9]+(ms|s|m|h|d|w|y).
                pattern: ^[0-9]+(ms|s|m|h|d|w|y)$
                type: string
              storage:
                description: Storage describes the per-replica PVC.
                properties:
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 20Gi
                    description: Size of the PVC, e.g. "50Gi".
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    type: string
                type: object
              thanos:
                description: Thanos optionally attaches a Thanos sidecar to each replica.
                properties:
                  enabled:
                    description: Enabled turns the sidecar on.
                    type: boolean
                  image:
                    default: quay.io/thanos/thanos:v0.37.2
                    description: Image is the Thanos container image.
                    type: string
                  objectStorageConfigSecretRef:
                    description: |-
                      ObjectStorageConfigSecretRef references a Secret with an
                      "objstore.yml" key in the Thanos objstore format. Sidecar reads it
                      via --objstore.config-file.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
            type: object
          status:
            description: status defines the observed state of PrometheusCluster
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the PrometheusCluster resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastBackupTime:
                description: LastBackupTime is the timestamp of the most recent successful
                  backup.
                format: date-time
                type: string
              lastFailoverTime:
                description: LastFailoverTime is the timestamp of the most recent
                  failover event.
                format: date-time
                type: string
              phase:
                description: Phase is the high-level lifecycle state.
                enum:
                - Provisioning
                - Active
                - Scaling
                - Failed
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of replicas reporting Ready.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
