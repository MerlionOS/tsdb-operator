apiVersion: observability.merlionos.org/v1
kind: PrometheusClusterSet
metadata:
  labels:
    app.kubernetes.io/name: tsdb-operator
    app.kubernetes.io/managed-by: kustomize
  name: prometheusclusterset-sample
spec:
  # TODO(user): Add fields here
